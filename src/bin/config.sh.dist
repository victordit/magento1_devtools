#!/bin/bash
# data remote for dump from prod or dev
# optional fields
remote_host="198.0.0.1"
remote_hostuser="remoteuser"
remote_hostpass="rpassword"
remote_dbuser="db_remote"
remote_dbpass="db_remotepwd"
remote_dbname="db_remotename"
remote_dbhost="localhost"

#data for import in local
dbhost="localhost"
dbuser="root"
dbpass="password"
dbname="dbname"
# remember the "trailing slash"
baseurl="http://mydomain.com/"

#
# optionals fields
#

#compile only these fields for change password for exist user
adminuser="admin"
adminpass="newpassword"

#compile also these fields for create user admin
adminemail="email@mydomain.com"
adminfirstname="Jhon"
adminlastname="Carter"
